# INERT — do not edit.
#
# This is the Cloudflare Pages redirect format. The site deploys to Vercel,
# which ignores this file entirely. Every rule now lives in /vercel.json
# ("redirects"), which is the single source of truth.
#
# Kept as a signpost rather than deleted, because two live redirect files that
# can drift apart is a worse failure than one empty one. If the site ever moves
# to Cloudflare Pages, regenerate this from vercel.json — do not maintain both.
